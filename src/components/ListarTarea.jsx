import React from 'react';
import { Form, Input, Button, Select, DatePicker } from 'antd';

const { Option } = Select;

const ListarTarea = () => {

    return (
        <div> Listar Tarea</div>
    );
};

export default ListarTarea;